# apiVersion: gateway.networking.k8s.io/v1
# kind: HTTPRoute
# metadata:
#   name: grafana-route
#   namespace: monitoring
#   annotations:
#     argocd.argoproj.io/sync-wave: "5" # This tells ArgoCD to wait for the stack (Wave 1)
# spec:
#   parentRefs:
#   - name: external-gateway-http
#     namespace: default
#   rules:
#   - matches:
#     - path:
#         type: PathPrefix
#         value: /grafana
#     backendRefs:
#     - name: victoria-metrics-stack-grafana
#       port: 80
